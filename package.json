{
  "name": "hinayuki64.github.io",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8000 --open=/ --watch=index.html,css,js,assets --ignore=node_modules,.git"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
